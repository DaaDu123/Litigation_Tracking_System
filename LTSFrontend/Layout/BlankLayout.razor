@* No sidebar/topbar/branding chrome - used by pages that render their own
   full-screen modal (backdrop + centered box), e.g. RequestToJoinFirm,
   so the modal is the entire visible page rather than sitting inside
   another page shell. *@
@inherits LayoutComponentBase

@Body
